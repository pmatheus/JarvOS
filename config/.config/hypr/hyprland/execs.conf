# Cursor
exec-once = hyprctl dispatch workspace 1
exec-once = hyprctl setcursor Bibata-Modern-Classic 24

# JarvOS shell: Omarchy provides the bar, wallpaper and notifications.
exec-once = systemctl --user start quickshell-jarvos.service

exec-once = jarvos-migrate --adopt

# First-run setup panel (no-op unless /var/lib/jarvos/first-run-pending exists)
exec-once = jarvos-setup --first-run

# Input method
exec-once = fcitx5

# Core components
exec-once = gnome-keyring-daemon --start --components=secrets
exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1 || /usr/libexec/polkit-gnome-authentication-agent-1 || /usr/lib/polkit-kde-authentication-agent-1 || /usr/libexec/polkit-kde-authentication-agent-1
exec-once = hypridle
exec-once = dbus-update-activation-environment --all
exec-once = sleep 1 && dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = hyprpm reload

# Audio
exec-once = easyeffects --gapplication-service

# Clipboard capture is owned by the Omarchy clipboard plugin.

# Auto-start apps
exec-once = [workspace special:ferdium silent] ferdium --enable-features=UseOzonePlatform --ozone-platform=wayland
