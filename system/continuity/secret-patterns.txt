# JarvOS continuity — content gate.
#
# Extended regexes (grep -E). A candidate file matching any of them is REFUSED,
# whatever its name, and reported as skipped. This is the backstop behind the
# path denylist: the denylist decides what we look at, this decides what may be
# written into the profile repo.
#
# Kept deliberately close to scripts/secret-scan.sh, which gates the public
# JarvOS repo. Same threat, two boundaries: that one guards what the distro
# publishes, this one guards what the user's private repo absorbs.
#
# One regex per line, '#' comments, blank lines ignored.

AKIA[0-9A-Z]{16}
gh[pousr]_[A-Za-z0-9]{20,}
github_pat_[A-Za-z0-9_]{20,}
sk-[A-Za-z0-9]{20,}
sk-ant-[A-Za-z0-9-]{20,}
xox[baprs]-[A-Za-z0-9-]{10,}
AIza[0-9A-Za-z_-]{30,}
glpat-[A-Za-z0-9_-]{15,}
dop_v1_[a-f0-9]{60,}
BEGIN (RSA|OPENSSH|EC|DSA|PGP) PRIVATE KEY
eyJ[A-Za-z0-9_-]{10,}\.[A-Za-z0-9_-]{10,}\.
(api[_-]?key|secret|password|passwd|token)["' ]*[:=][ ]*["'][A-Za-z0-9/+_-]{16,}["']
